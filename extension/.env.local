# Local dev stack (dev/docker-compose.yml). These are baked in at build time by
# shared/config.ts, so the loaded extension needs no manual configuration.
VITE_WS_ENDPOINT=ws://localhost:8080
VITE_DICTIONARY_BASE_URL=http://localhost:8081
